{#
    UI Element template
    type: quote
    element fields :
        author
        content
#}
{% if element is not empty %}
    <div>
        <blockquote>
            {{ element.content | raw | nl2br }}
            <br />— <cite>{{ element.author }}</cite>
        </blockquote>
    </div>
{% endif %}
